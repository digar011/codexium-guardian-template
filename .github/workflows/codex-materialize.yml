name: Codex Materializer (Guardian)

on:
  workflow_dispatch:
    inputs:
      task_id:
        description: "Codex task identifier (e.g. v4-docs-baseline)"
        required: true
        type: string
      target_path:
        description: "Target directory (e.g. docs)"
        required: true
        type: string
      files:
        description: |
          JSON object mapping filename -> file content.
          Example:
          {
            "README.md": "# Title",
            "SERVICES.md": "Content"
          }
        required: true
        type: string

permissions:
  contents: write
  pull-requests: write

# 🔐 Global safety switch (resolved at runtime)
env:
  NEO_AUTOMATION_ENABLED: ${{ secrets.NEO_AUTOMATION_ENABLED }}

jobs:
  materialize:
    name: Materialize Codex Output
    runs-on: ubuntu-latest

    steps:
      # 0️⃣ Guardian kill switch (MUST be first)
      - name: Guardian kill switch
        shell: bash
        run: |
          if [ "${NEO_AUTOMATION_ENABLED}" != "true" ]; then
            echo "❌ NEO automation is disabled. Exiting."
            exit 1
          fi
          echo "✅ NEO automation enabled. Continuing."

      # 1️⃣ Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: true
          fetch-depth: 0

      # 2️⃣ Clean up existing branch if it exists
      - name: Delete existing branch
        shell: bash
        continue-on-error: true
        run: |
          git push origin --delete codex/${{ inputs.task_id }} || true

      # 3️⃣ Validate JSON payload early
      - name: Validate JSON payload
        shell: bash
        run: |
          echo '${{ inputs.files }}' | jq type > /dev/null

      # 3️⃣ Create target directory
      - name: Create target directory
        shell: bash
        run: |
          mkdir -p "${{ inputs.target_path }}"

      # 4️⃣ Write files (hardened)
      - name: Write files
        shell: bash
        run: |
          set -euo pipefail
          
          # Parse and write each file
          while IFS= read -r entry; do
            # Skip empty entries
            if [ -z "$entry" ]; then
              continue
            fi
            
            # Extract filename and content using jq
            filename=$(echo "$entry" | jq -r '.key')
            content=$(echo "$entry" | jq -r '.value')
            
            # Validate filename
            if [ -z "$filename" ] || [ "$filename" = "null" ]; then
              echo "⚠️  Skipping entry with invalid key"
              continue
            fi
            
            # Normalize filename
            filename=$(echo "$filename" | xargs)
            filename="${filename#/}"
            filename="${filename%/}"
            
            # Skip if empty after normalization
            if [ -z "$filename" ]; then
              echo "⚠️  Skipping empty filename"
              continue
            fi
            
            # Build filepath
            filepath="${{ inputs.target_path }}/${filename}"
            
            # Verify it's not a directory
            if [ -d "$filepath" ]; then
              echo "⚠️  Skipping existing directory: $filepath"
              continue
            fi
            
            echo "📝 Writing: $filepath"
            
            # Create parent directory
            mkdir -p "$(dirname "$filepath")"
            
            # Write file
            printf '%s' "$content" > "$filepath"
            
            echo "✅ Created: $filepath"
          done < <(echo '${{ inputs.files }}' | jq -c 'to_entries[]')

      # 5️⃣ Verify files were created
      - name: Verify files were created
        shell: bash
        run: |
          echo "📋 Files in target directory:"
          ls -la "${{ inputs.target_path }}" || echo "Directory doesn't exist or is empty"
          
          echo ""
          echo "📊 Git status:"
          git status --short

      # 6️⃣ Create pull request (handles commit, push, and PR)
      - name: Create pull request
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ github.token }}
          commit-message: "codex: materialize ${{ inputs.task_id }}"
          branch: codex/${{ inputs.task_id }}
          delete-branch: true
          title: "Codex: ${{ inputs.task_id }}"
          body: |
            ## Codex Materialized Output

            **Task:** ${{ inputs.task_id }}
            **Target Path:** `${{ inputs.target_path }}`

            This PR was automatically generated by **Codex Guardian**.

            - Deterministic output only
            - V4 rules enforced
            - Human approval required
            - No auto-merge
          labels: |
            codex
            guardian
            automated
